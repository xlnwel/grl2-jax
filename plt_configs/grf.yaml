---
config:
  old: base
  # new: 0601-mujoco
  new: rnn_exp
rename:
  metrics/score: score
  metrics/dense_score: dense_score
  metrics/epslen: epslen
  train/group0_first_epoch/entropy: entropy
  group_first_epoch/entropy: entropy
  train/group0_first_epoch/clip_frac: first_epoch/clip_frac
  group_first_epoch/clip_frac: first_epoch/clip_frac
  train/group0_last_epoch/clip_frac: last_epoch/clip_frac
  group_last_epoch/clip_frac: last_epoch/clip_frac

data:
  # - date: 1127
  #   env_suite: grf
  # - date: "0223"
  # - date: "0228"
  - date: "0229"
